/**
 * Customer-visible version. Keep synchronized with the root VERSION file using
 * tools/set_release_version.ps1 before every installer/portable release.
 */
export const RELEASE_VERSION = '1.5.9';
export const RELEASE_LABEL = `v${RELEASE_VERSION} Commercial Edition`;
